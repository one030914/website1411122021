//const { createApp } = Vue;

/*const vueProfolio = createApp({
    data() {
        return {
            works: []  // 用來儲存作品資料
        };
    },
    created() {
        // 使用 jQuery 的 $.ajax 發送請求獲取資料
        $.ajax({
            url: "/works",  // 從後端 /works 路由獲取資料
            method: "GET",
            dataType: "json",
            success: (results) => {
                // 成功後將資料賦值給 Vue 實例的 works 屬性
                this.works = results;
            },
            error: (error) => {
                console.error("無法載入作品資料:", error);
            }
        });
    }
}).mount("#portfolio");*/
